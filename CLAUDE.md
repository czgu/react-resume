# Claude Code Instructions

## Package Manager

Use `yarn` instead of `npm` for all package management commands:

- Install dependencies: `yarn install`
- Add a package: `yarn add <package>`
- Run dev server: `yarn start` or `yarn dev`
- Build: `yarn build`
- Run tests: `yarn test`
